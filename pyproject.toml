[project]
name = "deep-symbolic-regression"
version = "0.1.0"
description = "Deep Symbolic Regression with Reinforcement Learning"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "torch",
    "numpy",
    "pandas",
    "matplotlib",
    "seaborn",
    "optuna"
]
